#include "main.h"

/**
 * _abs - this func computes absolute value
 *
 * @c: first value
 * Return: Always 0
 */

int _abs(int c)
{

	if (c < 0)
	{
		c = c * -1;
	}
	else
	{
		return (c);
	}
	return (c);
}
