#include "main.h"
/*
 * _abs - this function computes the absolute value
 * @c: first value
 * @d: decond value
 * Return: Always 0
 */

int _abs(int c)
{
	int d = -1;

	if (c < 0)
	{
		c = c * d;
	}
	else
	{
		return (c);
	}
	return (c);
}
