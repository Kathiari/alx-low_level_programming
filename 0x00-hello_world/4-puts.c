#include<stdio.h>
/*this is a program that displays strings*/
/**
 * main -the entry point
 *
 * Return: Always 0 succeses
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
